# 把這個檔案複製成同一層的 .env，再改你要改的那幾行。沒有 .env 也能跑，會用預設值。
#
#   cp .env.example .env        # macOS
#   copy .env.example .env      # Windows

# 重播倍速。兩份 case 的長度不一樣，所以各有各的倍速，讓兩張儀表板一輪都剛好 150 秒。
# Lab 06 的那兩晚共 41 小時 (147,600 秒 / 984) ，Lab 07 的事故窗 7 小時 55 分 (28,500 秒 / 190) 。
# 改了倍速就要一起改儀表板的時間窗 (現在是 now-150s) ，兩個數字要相等，畫面上才會剛好是
# 完整的一個 case 而且只有一份。想看慢一點就把兩個一起除以四 (246 與 48) ，
# 一輪變成 10 分鐘，時間窗跟著改成 now-10m。
WEEK6_REPLAY06_SPEED=984
WEEK6_REPLAY07_SPEED=190

# 重播程式多久把下一筆寫進 /metrics。Prometheus 也是每 0.5 秒抓一次，兩個對齊。
WEEK6_POLL_SECONDS=0.5

# Host ports。四個都是課程前幾週那一組加 10000 (3000 -> 13000、9090 -> 19090) ，
# 為的是不跟前幾週照 getting-started 裝在本機的 Grafana 與 Prometheus 搶 port。
# 只有在這四個也被佔用的時候才需要改這裡。
WEEK6_REPLAYER06_PORT=18011
WEEK6_REPLAYER07_PORT=18010
WEEK6_PROMETHEUS_PORT=19090
WEEK6_GRAFANA_PORT=13000

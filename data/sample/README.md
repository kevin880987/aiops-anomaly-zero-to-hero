# Sample Data

此資料夾放外部提供的原始範例資料，與 `data/synthetic/` 的模擬資料、`data/processed/` 的 pipeline 產物分開管理。

## Structure

- `mitake_error_log/`：三竹提供的 LibreNMS / RRDTool firewall port sample data，包含 `.rrd` 檔、讀取 helper 與讀檔 notebook。
